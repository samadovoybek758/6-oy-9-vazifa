import React, { useEffect, useState } from 'react'

function Card(props) {
    console.log(props);
   
    // return (proassName='container justify-center gap-4 mx-auto flex flex-row flex-wrap max-w-[1200px]'>
    //       {data.length > 0 && data.map(function (product) {
    //         return (
    //           <div key={product.id} className='flex flex-col p-3 mx-auto w-[300px] border border-solid rounded-md border-gray-600'>
    //             <h1>Name: {product.name}</h1>
    //             <h2>Price: ${product.price}</h2>
    //             <h2>Description: {product.description}</h2>
    //           </div>
    //         );
    //       })}
    //     </div>
      
    }
export default Card